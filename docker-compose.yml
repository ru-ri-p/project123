services:
  db:
    image: postgres:16
    container_name: attest-db
    environment:
      POSTGRES_USER: attest
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: attest
    ports:
      - "5433:5432"
    volumes:
      - attest_pgdata:/var/lib/postgresql/data

volumes:
  attest_pgdata:
