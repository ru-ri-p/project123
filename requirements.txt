fastapi[standard]>=0.141.1
uvicorn>=0.32.0
cryptography>=43.0.0
sqlalchemy>=2.0.36
psycopg2-binary>=2.9.10
alembic>=1.14.0
pydantic-settings>=2.6.0
requests>=2.32.0
rfc3161-client>=1.0.6
# Semantic rewrite drafting (app/services/rewrite.py). The feature is
# flag-gated on ANTHROPIC_API_KEY; without the key it simply does not exist.
anthropic>=1.1.0
argon2-cffi==25.1.0  # password hashing (argon2id) for dashboard logins — pinned, supply-chain discipline
